# jQuery example

The example installs the CryptoPro certificate provider as `$.gostCrypto`,
lists GOST certificates, signs text, and verifies the CMS signature.

```sh
npm install
npm run dev
```

The adapter receives the jQuery instance explicitly and does not require a
global `$`.
